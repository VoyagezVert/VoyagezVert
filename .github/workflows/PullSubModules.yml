name: Update Submodule

on:
  workflow_dispatch:

jobs:
  update-submodule:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout parent repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Update submodule and commit
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          cd vv_backend

          # Récupère le dernier commit de la branche staging du submodule
          git fetch origin staging
          git checkout origin/staging

          cd ..

          # Enregistre le nouveau commit dans le parent
          git add vv_backend
          git commit -m "🔄 Force update submodule to latest commit" || echo "No changes"
          git push
